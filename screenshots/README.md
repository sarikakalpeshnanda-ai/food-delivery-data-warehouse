# Screenshots

Add screenshots here after running the analysis queries. Good portfolio captures are:

1. The monthly KPI scorecard from `queries/02_analysis.sql`.
2. The city-level delivery-time result from `queries/02_analysis.sql`.
3. The cohort-retention result from `queries/03_advanced_queries.sql`.

Name files descriptively, for example `monthly-kpis.png`, `delivery-performance.png`, and `cohort-retention.png`.
